<Application x:Class="CanonPrintBridge.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>

            <!-- ===== Palette ===== -->
            <SolidColorBrush x:Key="Bg"      Color="#f3f3f3"/>
            <SolidColorBrush x:Key="Mica"    Color="#f6f7f9"/>
            <SolidColorBrush x:Key="Card"    Color="#ffffff"/>
            <SolidColorBrush x:Key="Card2"   Color="#fbfbfb"/>
            <SolidColorBrush x:Key="Stroke"  Color="#e6e8eb"/>
            <SolidColorBrush x:Key="Stroke2" Color="#d4d7db"/>
            <SolidColorBrush x:Key="Line"    Color="#eef0f2"/>
            <SolidColorBrush x:Key="Tx"      Color="#1a1a1a"/>
            <SolidColorBrush x:Key="Tx2"     Color="#5a5d61"/>
            <SolidColorBrush x:Key="Tx3"     Color="#8b8e92"/>
            <SolidColorBrush x:Key="Dis"     Color="#a9acb0"/>
            <SolidColorBrush x:Key="Accent"  Color="#005FB8"/>
            <SolidColorBrush x:Key="AccentFg" Color="#ffffff"/>
            <SolidColorBrush x:Key="AccentHover" Color="#1a6fbf"/>
            <SolidColorBrush x:Key="Ok"      Color="#0f7b0f"/>
            <SolidColorBrush x:Key="OkBg"    Color="#e6f4e6"/>
            <SolidColorBrush x:Key="OkBd"    Color="#a6dda3"/>
            <SolidColorBrush x:Key="Warn"    Color="#9a6700"/>
            <SolidColorBrush x:Key="WarnBg"  Color="#fff4ce"/>
            <SolidColorBrush x:Key="WarnBd"  Color="#eed58a"/>
            <SolidColorBrush x:Key="Off"     Color="#8b8e92"/>
            <SolidColorBrush x:Key="OffBg"   Color="#f1f2f4"/>
            <SolidColorBrush x:Key="OffBd"   Color="#dcdee1"/>
            <SolidColorBrush x:Key="Lost"    Color="#c42b1c"/>
            <SolidColorBrush x:Key="LostBg"  Color="#fdecec"/>
            <SolidColorBrush x:Key="LostBd"  Color="#f0b5b0"/>
            <SolidColorBrush x:Key="InfoBg"  Color="#eff5fd"/>
            <SolidColorBrush x:Key="InfoBd"  Color="#cadffa"/>
            <SolidColorBrush x:Key="Btn"     Color="#fbfbfb"/>
            <SolidColorBrush x:Key="BtnBd"   Color="#d3d5d8"/>
            <SolidColorBrush x:Key="BtnHover" Color="#f3f4f5"/>
            <SolidColorBrush x:Key="FieldLocked" Color="#f4f5f6"/>

            <FontFamily x:Key="Font">Segoe UI Variable Text, Segoe UI</FontFamily>
            <FontFamily x:Key="Mono">Cascadia Mono, Consolas</FontFamily>

            <!-- ===== Default Button ===== -->
            <Style TargetType="Button">
                <Setter Property="FontFamily" Value="{StaticResource Font}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource Tx}"/>
                <Setter Property="Background" Value="{StaticResource Btn}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BtnBd}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Height" Value="34"/>
                <Setter Property="Padding" Value="14,0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource BtnHover}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="bd" Property="Opacity" Value="0.8"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bd" Property="Background" Value="#e9eaec"/>
                                    <Setter TargetName="bd" Property="BorderBrush" Value="#e3e4e6"/>
                                    <Setter Property="Foreground" Value="{StaticResource Dis}"/>
                                    <Setter Property="Cursor" Value="No"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===== Accent Button ===== -->
            <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="{StaticResource Accent}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
                <Setter Property="Foreground" Value="{StaticResource AccentFg}"/>
                <Setter Property="Padding" Value="16,0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentHover}"/>
                                    <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentHover}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bd" Property="Background" Value="#e9eaec"/>
                                    <Setter TargetName="bd" Property="BorderBrush" Value="#e3e4e6"/>
                                    <Setter Property="Foreground" Value="{StaticResource Dis}"/>
                                    <Setter Property="Cursor" Value="No"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===== Subtle toolbar button ===== -->
            <Style x:Key="SubtleButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource Tx2}"/>
                <Setter Property="Padding" Value="12,0"/>
            </Style>

            <!-- ===== Icon Button ===== -->
            <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource Tx2}"/>
                <Setter Property="Width" Value="34"/>
                <Setter Property="Height" Value="34"/>
                <Setter Property="Padding" Value="0"/>
            </Style>

            <!-- ===== Danger Button ===== -->
            <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource AccentButton}">
                <Setter Property="Background" Value="{StaticResource Lost}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Lost}"/>
            </Style>

            <!-- ===== Card Border ===== -->
            <Style x:Key="Card" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource Card}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Stroke}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
            </Style>

            <!-- ===== Section header ===== -->
            <Style x:Key="SectionHeader" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource Font}"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource Tx2}"/>
            </Style>

            <!-- ===== Field label ===== -->
            <Style x:Key="FieldLabel" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource Font}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Foreground" Value="{StaticResource Tx2}"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- ===== TextBox ===== -->
            <Style TargetType="TextBox">
                <Setter Property="FontFamily" Value="{StaticResource Font}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Foreground" Value="{StaticResource Tx}"/>
                <Setter Property="Background" Value="{StaticResource Card}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Stroke2}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="Padding" Value="8,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="CaretBrush" Value="{StaticResource Accent}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="bd" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"/>
                                    <Border x:Name="focusLine" Height="2" VerticalAlignment="Bottom"
                                            Background="{StaticResource Accent}" CornerRadius="0,0,5,5"
                                            Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="focusLine" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource FieldLocked}"/>
                                    <Setter Property="Foreground" Value="{StaticResource Tx3}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Monospace read-only path field -->
            <Style x:Key="PathField" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                <Setter Property="FontFamily" Value="{StaticResource Mono}"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="IsReadOnly" Value="True"/>
            </Style>

            <!-- ===== ComboBox ===== -->
            <ControlTemplate x:Key="ComboToggle" TargetType="ToggleButton">
                <Border x:Name="bd" Background="{StaticResource Card}"
                        BorderBrush="{StaticResource Stroke2}" BorderThickness="1" CornerRadius="5">
                    <Path x:Name="arrow" HorizontalAlignment="Right" VerticalAlignment="Center"
                          Margin="0,0,10,0" Fill="{StaticResource Tx3}"
                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="bd" Property="Background" Value="{StaticResource Card2}"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <Style TargetType="ComboBox">
                <Setter Property="FontFamily" Value="{StaticResource Font}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Foreground" Value="{StaticResource Tx}"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="tb" Focusable="False"
                                              Template="{StaticResource ComboToggle}"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <ContentPresenter x:Name="cp" Margin="10,0,26,0" VerticalAlignment="Center"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>
                                <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}" Focusable="False"
                                       PopupAnimation="Fade">
                                    <Border Background="{StaticResource Card}" BorderBrush="{StaticResource Stroke}"
                                            BorderThickness="1" CornerRadius="6" Margin="0,4,0,0"
                                            MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="18" ShadowDepth="4" Opacity="0.16" Color="#101828"/>
                                        </Border.Effect>
                                        <ScrollViewer MaxHeight="260">
                                            <StackPanel IsItemsHost="True" Margin="4"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="{StaticResource Tx3}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="FontFamily" Value="{StaticResource Font}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Foreground" Value="{StaticResource Tx}"/>
                <Setter Property="Padding" Value="9,7"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="bd" Background="Transparent" CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource Line}"/>
                                </Trigger>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource Line}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===== ProgressBar ===== -->
            <Style TargetType="ProgressBar">
                <Setter Property="Height" Value="6"/>
                <Setter Property="Foreground" Value="{StaticResource Accent}"/>
                <Setter Property="Background" Value="{StaticResource OffBg}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ProgressBar">
                            <Border Background="{TemplateBinding Background}" CornerRadius="3" ClipToBounds="True">
                                <Grid x:Name="root">
                                    <Rectangle x:Name="PART_Track"/>
                                    <Decorator x:Name="PART_Indicator" HorizontalAlignment="Left">
                                        <Rectangle x:Name="det" Fill="{TemplateBinding Foreground}" RadiusX="3" RadiusY="3"/>
                                    </Decorator>
                                    <Rectangle x:Name="ind" Fill="{TemplateBinding Foreground}" RadiusX="3" RadiusY="3"
                                               Width="90" HorizontalAlignment="Left" Visibility="Collapsed"
                                               RenderTransformOrigin="0,0">
                                        <Rectangle.RenderTransform>
                                            <TranslateTransform x:Name="tt" X="-120"/>
                                        </Rectangle.RenderTransform>
                                    </Rectangle>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsIndeterminate" Value="True">
                                    <Setter TargetName="det" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="ind" Property="Visibility" Value="Visible"/>
                                    <Trigger.EnterActions>
                                        <BeginStoryboard Name="sb">
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation Storyboard.TargetName="tt" Storyboard.TargetProperty="X"
                                                                 From="-120" To="520" Duration="0:0:1.4"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <StopStoryboard BeginStoryboardName="sb"/>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===== Expander ===== -->
            <Style TargetType="Expander">
                <Setter Property="FontFamily" Value="{StaticResource Font}"/>
                <Setter Property="Foreground" Value="{StaticResource Tx2}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Expander">
                            <Border Style="{StaticResource Card}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <ToggleButton x:Name="hdr" Grid.Row="0" Cursor="Hand"
                                                  IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border x:Name="hb" Background="Transparent" Padding="13,11" CornerRadius="8">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Path x:Name="chev" Width="10" Height="12"
                                                              Fill="{StaticResource Tx2}" VerticalAlignment="Center"
                                                              Data="M 0 0 L 5 5 L 0 10 Z"
                                                              RenderTransformOrigin="0.4,0.5">
                                                            <Path.RenderTransform>
                                                                <RotateTransform Angle="0"/>
                                                            </Path.RenderTransform>
                                                        </Path>
                                                        <ContentPresenter Margin="9,0,0,0" VerticalAlignment="Center"
                                                                          ContentSource="Header"/>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="chev" Property="RenderTransform">
                                                            <Setter.Value>
                                                                <RotateTransform Angle="90"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="hb" Property="Background" Value="{StaticResource Card2}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <Border x:Name="content" Grid.Row="1" Visibility="Collapsed"
                                            BorderBrush="{StaticResource Line}" BorderThickness="0,1,0,0">
                                        <ContentPresenter Margin="0"/>
                                    </Border>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsExpanded" Value="True">
                                    <Setter TargetName="content" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===== ToolTip ===== -->
            <Style TargetType="ToolTip">
                <Setter Property="FontFamily" Value="{StaticResource Font}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToolTip">
                            <Border Background="#2b2b2b" CornerRadius="6" Padding="10,7" MaxWidth="240">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="18" ShadowDepth="6" Opacity="0.28" Color="Black"/>
                                </Border.Effect>
                                <ContentPresenter/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===== Status dots ===== -->
            <Style x:Key="DotBase" TargetType="Ellipse">
                <Setter Property="Width" Value="12"/>
                <Setter Property="Height" Value="12"/>
            </Style>
            <Style x:Key="DotOk" TargetType="Ellipse" BasedOn="{StaticResource DotBase}">
                <Setter Property="Fill" Value="{StaticResource Ok}"/>
                <Setter Property="Effect">
                    <Setter.Value><DropShadowEffect BlurRadius="7" ShadowDepth="0" Color="#0f7b0f" Opacity="0.5"/></Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="DotBooting" TargetType="Ellipse" BasedOn="{StaticResource DotBase}">
                <Setter Property="Fill" Value="{StaticResource Warn}"/>
                <Setter Property="Effect">
                    <Setter.Value><DropShadowEffect BlurRadius="7" ShadowDepth="0" Color="#9a6700" Opacity="0.5"/></Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="DotOff" TargetType="Ellipse" BasedOn="{StaticResource DotBase}">
                <Setter Property="Fill" Value="{StaticResource Off}"/>
                <Setter Property="Effect">
                    <Setter.Value><DropShadowEffect BlurRadius="6" ShadowDepth="0" Color="#8b8e92" Opacity="0.45"/></Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="DotLost" TargetType="Ellipse" BasedOn="{StaticResource DotBase}">
                <Setter Property="Fill" Value="{StaticResource Lost}"/>
                <Setter Property="Effect">
                    <Setter.Value><DropShadowEffect BlurRadius="7" ShadowDepth="0" Color="#c42b1c" Opacity="0.5"/></Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
